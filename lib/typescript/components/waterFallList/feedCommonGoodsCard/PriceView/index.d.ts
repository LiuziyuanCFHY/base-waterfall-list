import React from 'react';
import { FeedGoodsInfo } from '../../model';
interface IProps {
    goodsInfo: FeedGoodsInfo;
    maxWidth: number;
}
export declare const PriceView: React.MemoExoticComponent<({ goodsInfo, maxWidth }: IProps) => JSX.Element>;
export {};
